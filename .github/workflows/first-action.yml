name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
      - name: Print greeting
        run: echo "Hello World!"
      -name: Print goodbye
        run: "Done - bye!"
